# CLAUDE.md

Guidance for AI coding agents working in this repo. Complements — does not replace — [AGENTS.md](AGENTS.md) (which holds product vision, rules, roadmap). This file documents **how the code actually looks today** so an agent can navigate without re-exploring.

---

## Product in one paragraph

Commercial web service that generates an **IDP Companion PDF** (a courtesy translation/companion document — *not* an official government International Driving Permit). Landing → SPA form → OCR verify → payment → PDF download. Sold as one-off purchase per plan duration (1 / 3 / 5 years).

MVP launch target: ~1 month. Solo dev (@flyonk). Bias toward "simple but reliable" over "elegant but complex". Do not overengineer.

---

## Monorepo layout

pnpm workspaces. Root scripts: `pnpm dev`, `pnpm build`, `pnpm dev:frontend`, `pnpm dev:backend`.

```
apps/
  backend/     NestJS + Prisma + PostgreSQL           (:3001)
  frontend/    Vue 3 + Vite + Pinia + Tailwind v4     (SPA, :5173)
  landing/     Nuxt 4 SSR + Tailwind v4               (marketing)
packages/
  ui-kit/      Shared components + design tokens (consumed via @ui-kit alias)
  i18n/        Shared locale constants (SUPPORTED_LOCALES, query param, storage key)
```

---

## End-to-end flow (as actually wired)

1. **Landing** ([apps/landing/components/widgets/StartApplicationWidget.vue](apps/landing/components/widgets/StartApplicationWidget.vue)) — user picks country + plan, click builds `${appUrl}/apply?issueCountry=XX&planYears=N&locale=xx` and redirects to the SPA. Stateless URL handoff — no cookie/session.

2. **SPA `/apply`** ([apps/frontend/src/pages/DriverLicenseApplicationPage.vue](apps/frontend/src/pages/DriverLicenseApplicationPage.vue)) — single long page that renders Step 1 (form + uploads + signature) and Step 3 (download after payment).
   - Router reads `?locale=` once and persists to `localStorage['idp.locale']` ([apps/frontend/src/app/router/index.ts:37-69](apps/frontend/src/app/router/index.ts#L37-L69)).
   - License image goes to `POST /api/verify/license` (OCR, returns `passed|review|failed` + `verificationId`).
   - Form submit → `POST /api/applications` (multipart; license `verificationId`, headshot file, form fields) → returns `{ applicationId, nextAction: 'payment' }`.
   - Router pushes `/payment/:applicationId`.

3. **SPA `/payment/:id`** ([apps/frontend/src/pages/PaymentPage.vue](apps/frontend/src/pages/PaymentPage.vue)) — **today: Gumroad-only, hardcoded URLs per plan** (lines ~140-194). Listens for `window.onGumroadPurchaseComplete` client-side callback to flip `isPaid = true`. No polling, no webhook awareness on the client. After confirmation, downloads via `GET /api/applications/:id/pdf?disposition=attachment`.

4. **Backend** ([apps/backend/src/applications/applications.service.ts](apps/backend/src/applications/applications.service.ts)) — persists the Application row with `status: 'created'`; runs face-match (AWS Rekognition) between license photo and headshot.

5. **Payment confirmation (backend)** — Gumroad → `POST /api/payments/gumroad/webhook?token=...` ([apps/backend/src/payments/providers/gumroad-webhook.controller.ts](apps/backend/src/payments/providers/gumroad-webhook.controller.ts)) → flips `Application.status` to `paid`, optionally emails PDF via Resend.

6. **PDF gating** ([apps/backend/src/applications/applications.service.ts:157-162](apps/backend/src/applications/applications.service.ts#L157-L162)) — if `REQUIRE_PAYMENT_FOR_PDF=1` and status is not `paid`/`pdf_ready` → 403. Otherwise PDF is generated on-demand with `pdf-lib` (no disk cache).

---

## Backend — key modules

[apps/backend/src/app.module.ts](apps/backend/src/app.module.ts) wires:

| Module | Endpoints | Notes |
|---|---|---|
| `PrismaModule` | — | Single Prisma client |
| `VerifyModule` | `POST /api/verify/license` | Tesseract.js OCR; writes to `uploads/verify/`; TTL 1h; state in in-memory `VerificationStore` |
| `ReferenceModule` | `GET /api/reference/{countries,pricing}` | Countries data generated by `npm run countries:generate` |
| `ApplicationsModule` | `POST /api/applications`, `GET /api/applications/:id/pdf` | Persists Application; generates PDF on demand |
| `PaymentsModule` | `POST /api/payments/gumroad/webhook` | Gumroad only (today) |
| `EmailModule` | — | Resend client; event-based tags |
| `FaceVerificationModule` | — | AWS Rekognition `CompareFaces`; threshold via `FACE_MATCH_SIMILARITY_THRESHOLD` |

### Prisma schema ([apps/backend/prisma/schema.prisma](apps/backend/prisma/schema.prisma))

One table: `Application`. No `Payment` table — **payment state lives solely in `Application.status`**.

```prisma
enum ApplicationStatus { created  paid  pdf_ready }
```

Gumroad sale metadata is **not persisted** (only emitted as Resend tags). Adding a real audit trail requires a new `Payment` table — worth doing when a second provider (e.g. crypto) lands.

### Backend commands ([apps/backend/package.json](apps/backend/package.json))

```bash
npm run start:dev           # watch mode, :3001
npm run build
npm run prisma:migrate      # prisma migrate dev
npm run prisma:generate
npm run prisma:studio
npm test
npm run countries:generate  # regenerate countries dataset
```

---

## Frontend — FSD layers (actually in use)

Feature-Sliced Design — respect the boundary: never import from a higher layer.

```
src/
  app/          router, i18n, entrypoint (main.ts, App.vue)
  pages/        DriverLicenseApplicationPage.vue, PaymentPage.vue
  widgets/      DriverApplicationLayout, StepProgressHeader, SummarySidebar
  features/     driver-application, driver-details, license-categories,
                plan-years, verify-license
  entities/     driver-application (Pinia store + types)
  shared/       api (httpClient, apiClient), ui primitives, utils
```

### State — single Pinia store

[apps/frontend/src/entities/driver-application/model/store.ts](apps/frontend/src/entities/driver-application/model/store.ts) — `useDriverApplicationStore` holds `selectedYears`, `driver`, `verify`, `applicationId`. Nothing else is stored globally.

### API client

- [apps/frontend/src/shared/api/httpClient.ts](apps/frontend/src/shared/api/httpClient.ts) — single Axios instance, `VITE_API_BASE_URL` default `/api` (vite dev proxies `/api` → `http://localhost:3001`).
- [apps/frontend/src/shared/api/apiClient.ts](apps/frontend/src/shared/api/apiClient.ts) — typed wrappers `apiGet/apiPost/apiGetBlob` with `toApiError()` normalisation.

### Frontend commands ([apps/frontend/package.json](apps/frontend/package.json))

```bash
npm run dev       # vite, :5173
npm run build     # vue-tsc -b && vite build
npm run preview
```

---

## Landing — Nuxt (marketing + SEO)

[apps/landing/nuxt.config.ts](apps/landing/nuxt.config.ts).

- i18n: `@nuxtjs/i18n`, strategy `prefix_except_default`. Shipped locales: **en, ru, es** — German planned but not implemented. Locale files: [apps/landing/i18n/locales/](apps/landing/i18n/locales/).
- SEO: `@nuxtjs/sitemap` generates `sitemap.xml` (legal pages excluded). [apps/landing/public/robots.txt](apps/landing/public/robots.txt) allows crawl, disallows `/api/`. OG meta, canonical, hreflang, JSON-LD schemas wired in `app.vue`.
- Pricing: fetched from `/api/pricing` with hardcoded fallback in [apps/landing/composables/usePricing.ts](apps/landing/composables/usePricing.ts).

---

## i18n

Locales shipped repo-wide: **en / ru / es**. German is in AGENTS.md roadmap but **not implemented**.

- Shared source of truth: [packages/i18n/index.ts](packages/i18n/index.ts) — `SUPPORTED_LOCALES`, `LOCALE_QUERY_PARAM = 'locale'`, `LOCALE_STORAGE_KEY = 'idp.locale'`.
- SPA: Vue-i18n v11, messages at [apps/frontend/src/app/i18n/messages/](apps/frontend/src/app/i18n/messages/). Router keeps `?locale=` in URL for shareability + writes to localStorage.
- Landing: Nuxt i18n, messages at [apps/landing/i18n/locales/](apps/landing/i18n/locales/).

Landing → SPA passes `locale` via query string.

---

## UI kit ([packages/ui-kit](packages/ui-kit))

Consumed by both landing and SPA via Vite alias `@ui-kit`.

- Components: `BaseButton`, `BaseInput`, `BaseSelect`, `BaseCountrySelect`, `BaseFile`, `AppHeader`, `AppFooter`, `LocaleSwitcher`, `MobileNavMenu`.
- Tokens: mint `#CBF3F0`, sea `#2EC4B6`, orange `#FF9F1C`, slate grays. Radii 16/24px. Section gaps 48/64px. Files: `css-vars.scss`, `variables.scss`, `tailwind.preset.ts`, `layout.scss`.
- Layout helpers: `.container` (max 1280px, responsive padding), `.section`.

---

## Styling — Tailwind + BEM-like (from AGENTS.md)

Use ~70% of Tailwind. Give widgets/wrappers BEM-like classes (`legal-page`, `legal-section__heading`, `header-inner`). Don't stuff everything into a single Tailwind string. Mix semantic blocks + Tailwind utilities. Prefer `.container` / `.section` from ui-kit.

---

## File storage

- License images — `uploads/verify/<uuid>.<ext>`, **TTL 1 hour** (VerificationStore cleans up).
- Headshots — `uploads/<ts-rand>.<ext>`, **no cleanup yet** (roadmap item). Path persisted in `Application.headshotPath`.
- PDFs — generated in-memory with pdf-lib, never written to disk.
- Base dir: `UPLOAD_DIR` env, default `./uploads`.

---

## Environment variables (payment/PDF related)

```bash
# Gumroad
GUMROAD_WEBHOOK_TOKEN=...
REQUIRE_PAYMENT_FOR_PDF=false        # '1' or 'true' to enforce
EMAIL_SEND_ON_PAYMENT=0

# Email (Resend)
RESEND_API_KEY=...
EMAIL_SEND_ON_APPLICATION_CREATE=0

# Storage
UPLOAD_DIR=./uploads

# Face verification (AWS Rekognition)
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
AWS_REGION=eu-central-1
FACE_MATCH_SIMILARITY_THRESHOLD=85

# Verify (OCR)
VERIFY_TTL_MS=3600000
VERIFY_CLEANUP_MS=600000
VERIFY_OCR_MAX_ATTEMPTS=5
```

See [apps/backend/.env.example](apps/backend/.env.example) for the authoritative list.

---

## Known gaps / tech-debt hotspots

Calling these out so an agent doesn't "rediscover" them and churn on fixes:

1. **Payment is Gumroad-coupled.** No `IPaymentProvider` abstraction. URLs are hardcoded on the frontend, single webhook controller on the backend. Adding a second provider (crypto, Paddle) means either (a) a parallel controller + a new frontend branch in `PaymentPage.vue`, or (b) a small abstraction refactor (provider registry + strategy). Pick (b) if you're adding a provider anyway — (a) is a short-term trap.
2. **No `Payment` table.** Gumroad sale_id is only a Resend email tag. Adding a second payment provider = perfect moment to persist `Payment { id, applicationId, provider, externalId, amount, currency, status, raw, createdAt }`.
3. **Webhook auth is a query-string shared secret**, not HMAC. Upgrade to HMAC when touching this area (standard for Paddle; required for most crypto gateways).
4. **No headshot cleanup.** Storage will grow unbounded. Low priority for MVP, high priority post-launch.
5. **German locale missing.** Trivial to add once strings are translated.
6. **PDF gating is a status check in service code.** Fine for MVP. If/when you touch auth, promote to a NestJS Guard.

---

## Coding rules (from AGENTS.md — condensed)

- TypeScript strict. Avoid `any`. Explicit types preferred.
- NestJS: DTO validation (class-validator).
- Frontend: Zod for runtime validation; vee-validate for forms.
- Respect FSD boundaries — no imports from a higher layer; flag violations, don't silently move files.
- Preserve existing API contracts unless explicitly asked to change them.
- Minimal changes. No new deps unless necessary.
- If unsure about architecture — ask.

---

## Hand-off tips for agents

- **Exploring?** Start with this file + `AGENTS.md` + [docs/seo-docs/SEO_STRATEGY.md](docs/seo-docs/SEO_STRATEGY.md). The Explore agent is overkill for narrow questions — use Grep/Glob directly.
- **Changing payment?** Read [apps/frontend/src/pages/PaymentPage.vue](apps/frontend/src/pages/PaymentPage.vue) + [apps/backend/src/payments/providers/gumroad-webhook.controller.ts](apps/backend/src/payments/providers/gumroad-webhook.controller.ts) together — they're coupled by the applicationId-in-referrer convention.
- **Changing i18n?** Touch three places: `packages/i18n/index.ts` (constants), `apps/frontend/src/app/i18n/messages/*.ts`, `apps/landing/i18n/locales/*.json`.
- **Adding a backend module?** Wire it in [apps/backend/src/app.module.ts](apps/backend/src/app.module.ts); prefix routes with `/api/...` (global prefix is set in `main.ts`).

---

## SEO — landing-specific conventions

> Comprehensive strategy: [docs/seo-docs/SEO_STRATEGY.md](docs/seo-docs/SEO_STRATEGY.md). What follows is the **how-it-actually-works** side for the agent.

### Page types and where they live

| Type | Location | Example | Status |
|---|---|---|---|
| Marketing pages | `apps/landing/pages/*.vue` | `index.vue`, `pricing.vue`, `privacy-policy.vue` | live |
| Country-pair (Tier 1/2) | `apps/landing/pages/idp-for-{origin}-drivers-in-{destination}.vue` | `idp-for-russian-drivers-in-thailand.vue` | first one live 2026-04-25 |
| Long-form guides (Tier 3) | `apps/landing/pages/guides/*.vue` | `driving-abroad-complete-guide-2026.vue` | not built yet |

URL pattern is a contract — **never change** `/idp-for-{origin}-drivers-in-{destination}/` once a page is published. Canonicals depend on it. Same for slugs.

### Hosting and deploy

- Landing builds via `nuxt generate` (static) and is deployed to **Cloudflare Pages** (project name `idp-companion-landing`, GitHub repo `flyonkDev/dl-translation-service`).
- **Auto-deploy on push to `master`.** No manual step. Validate prod after push by `curl https://idpcompanion.com/<path>` (apex resolves for any client; www is sometimes blocked by CF for non-verified bots — see Lessons below).
- Both `idpcompanion.com` (apex, CNAME-flattened) and `www.idpcompanion.com` (CNAME → apex) are configured as Custom Domains on the Pages project. Canonical URLs use `www`.

### What's already in place (Phase 0)

- SSR + sitemap (via `@nuxtjs/sitemap`) generated at `/sitemap_index.xml`
- `robots.txt` static at [apps/landing/public/robots.txt](apps/landing/public/robots.txt) — Cloudflare's "Manage robots.txt" feature should be **disabled** so it doesn't override
- `@nuxtjs/i18n` with hreflang for `en/ru/es`. Default locale `en` (no prefix). Russian at `/ru/...`, Spanish at `/es/...`
- Per-page `useSeoMeta`: full OG (image:width/height/type/alt/secure_url) + Twitter Cards + canonical
- `og:locale` mapping via `apps/landing/utils/ogLocale.ts` — converts `en` → `en_US` (OG spec needs `xx_XX`)
- JSON-LD on home: `Organization` + `WebSite` + `SoftwareApplication` + `FAQPage`
- JSON-LD on pricing: `Product` + `AggregateOffer` (+ per-plan Offers) + `BreadcrumbList` + `FAQPage`
- Static OG image at `/og-image.png` (fallback). **Per-page dynamic OGs** are generated by `nuxt-og-image` (see below).
- Static SEO illustrations at [apps/landing/public/illustrations/](apps/landing/public/illustrations/) — country-pair hero artwork, etc.

### nuxt-og-image (dynamic per-page OG)

- Module: `nuxt-og-image` configured in [apps/landing/nuxt.config.ts](apps/landing/nuxt.config.ts)
- Templates live at `apps/landing/components/OgImage/*.vue` (rendered via Satori → PNG at request time, cached by CF)
- Per-page usage: `defineOgImageComponent('CountryPair', { origin, destination })` inside the page's `<script setup>` — emits `<meta property="og:image" content="/__og-image__/.../{slug}/og.png">`
- Test locally: visit `/__og-image__/static/<route>/og.png` to inspect the generated image
- Don't fight it — when adding a new country-pair page, just call `defineOgImageComponent`. Generic pages (privacy/refund/terms) use the static `/og-image.png` fallback.

### Country-pair page architecture (current — post-refactor 2026-04-26)

Country-pair pages use a **typed TS content + thin Vue wrapper + shared component** pattern. Do NOT write monolithic per-page .vue files (the original russia-thailand.vue used to be 870+ lines — that's why we refactored).

**Three files per page:**

1. **Content** — `apps/landing/content/country-pairs/{slug}.ts`
   - Typed `CountryPairCopy` (defined in [russia-thailand.ts](apps/landing/content/country-pairs/russia-thailand.ts))
   - Exports `Record<locale, CountryPairCopy>`, e.g. `usItalyCopy: Record<'en', CountryPairCopy> = { en }`
   - **Multi-locale lives here**, not in `i18n/locales/*.json`. For an EN+ES page (e.g. us-spain), provide both keys: `{ en, es }`
2. **Page wrapper** — `apps/landing/pages/idp-for-{origin}-drivers-in-{destination}.vue`
   - 10–25 lines. Just `defineI18nRoute({ locales: [...] })` + `<CountryPairPage>` with props
   - Canonical reference: [pages/idp-for-us-drivers-in-mexico.vue](apps/landing/pages/idp-for-us-drivers-in-mexico.vue)
3. **Shared renderer** — [components/CountryPair/Page.vue](apps/landing/components/CountryPair/Page.vue)
   - Renders all sections, handles `useSeoMeta`, `defineOgImageComponent`, JSON-LD (Article + FAQPage + BreadcrumbList + Product + optional HowTo)
   - Picks copy by current locale, falls back to first key if missing
   - **Don't edit per page** — this is shared. Change here only when the structural pattern needs to change.

**Required props on `<CountryPairPage>`:** `copy-by-locale`, `origin-flag` (ISO-2 lowercase), `destination-flag` (ISO-2 lowercase), `issue-country` (ISO-2 uppercase, passed to `/apply`), `canonical-path`, `id-prefix` (FAQ a11y), `illustration` (`/illustrations/{slug}-hero.svg`), `analytics-origin`, `analytics-destination`.

**URL pattern is a contract** — never change `/idp-for-{origin}-drivers-in-{destination}/` slug once published. Filename, canonical, internal links, and external backlinks all depend on it.

**Locale policy** (validated across 5 live pages):
- Default: **EN only** for any US-origin page (us-italy, us-greece, us-spain, us-japan, us-mexico are all EN-only — exception: us-spain ships EN+ES because it explicitly targets the 60M+ Hispanic-American audience)
- Russia-origin: **EN + RU** (russia-thailand)
- Rule: ship the origin's primary language only when there's a clear audience signal. Don't add ES to a US→Japan page just because we *can*; the searcher intent isn't there.
- Filter via `defineI18nRoute({ locales: ['en'] })` in the page wrapper.

### Tier 1 country-pair playbook (validated 2026-04-26)

The 5 live pages ([us-italy](apps/landing/content/country-pairs/us-italy.ts), [us-greece](apps/landing/content/country-pairs/us-greece.ts), [us-spain](apps/landing/content/country-pairs/us-spain.ts), [us-japan](apps/landing/content/country-pairs/us-japan.ts), [us-mexico](apps/landing/content/country-pairs/us-mexico.ts)) are the canonical references. **Always read 2–3 of them before authoring a new one** — they encode the patterns below.

**Required `CountryPairCopy` blocks** (no v-if in the template — must provide):
`seo`, `breadcrumbs`, `hero`, `quickAnswer`, `whyNotEnough` (3 reasons), `rules` (8 cards), `fines` (7–9 rows), `honesty` (4 sub-blocks: is/isNot/helps/needOfficial), `renting` (4 chains + 6+ tips), `outcomes` (5 rows), `faq` (8–10 items), `related` (6 items), `finalCta`, `legal`, `og`.

**Optional blocks for Tier 1 — include all that apply** (this is what separates Tier 1 from Tier 2):
- `lastReviewed: 'Month YYYY'` — renders as freshness pill. Always include.
- `tldr` — 3-row table comparing US License vs AAA/AATA IDP vs IDP Companion in destination context. `tone: 'neutral' | 'official' | 'companion'` colors the row. **Always include.**
- `lez` — country-specific deep-dive zones (Italy ZTL, Mexico Quintana Roo checkpoints, Milan Area B/C). Renders as gradient block with badge. Use whenever the destination has documented enforcement zones / camera traps / restriction systems.
- `rejects` — country-specific list of things the destination *rejects* (Japan's 4 IDP types it rejects, etc.). Use when the destination has notable disqualifying factors.
- `alphabet` — non-Latin script destinations (Japan, Greece, Russia, Thailand). Examples + how-it-helps list.
- `phrases` — 8 phrases in the destination's primary language with English translation + context. **Always include for Tier 1.** This is one of the highest-engagement sections (validated by GA scroll-depth).
- `howTo` — 5 steps + `schemaName`/`duration`/`cost` for HowTo JSON-LD. Always include — generates additional rich-result eligibility.
- `labels.freshnessPrefix` — locale override for "Last reviewed:" prefix. Default handles en/es/ru.

**Honesty block reframing rules:**
- `honesty.needOfficial` lists **documents the user must carry alongside IDP Companion** — physical home-country license, passport, motorcycle endorsement (where relevant), local insurance card, FMM/visa stamp. **Never list competitor IDP issuers (AAA, AATA, etc.) here.** Title patterns: "What you should carry alongside IDP Companion" or "Documents the local law cares about".
- Countries that **don't require** an IDP (Mexico): same `needOfficial` pattern — list local-law documents (physical license, TPL insurance, passport, FMM). **Don't pretend an IDP is needed when it isn't** — Google's helpful-content classifier punishes this and AGENTS.md forbids it.

**Positioning rules — binding (per AGENTS.md):**
- **Authorized national IDP issuers (AAA, AATA, РОСАВТОКЛУБ, ADAC, RACE, ACI, etc.) appear in the legal disclaimer ONLY** — single brief sentence disclosing we are not them. They do **not** appear in `tldr` comparison rows, `honesty` recommendations, `howTo` step text, `faq` answers as alternatives, `finalCta`, or anywhere in the main conversion flow.
- **`tldr` table** compares **US License (alone) vs IDP Companion** — two rows, not three. Do not add a "competitor IDP" row. The comparison's job is to show why IDP Companion solves the friction the bare US license creates.
- **No "When you should also carry an AAA IDP" lists**, no "AAA route works fine if X" footnotes, no "Get AAA →" links anywhere on the page (including FAQ answers and final CTA helper text).
- **Treat the page as a sales funnel.** The reader landed via a search query. The page's job is to inform honestly about the destination + close the sale on IDP Companion. Confident, fact-based, source-cited (academic + government + travel-industry sources). Never scammy (no fake seals, no fake urgency).
- **Honest disclosures stay** — `honesty.is`, `honesty.isNot` keep the "we are a private translation document, not a government IDP" disclosure (legal floor + brand differentiation from scammy IDA-style sites). These do **not** redirect users to competitors — they disclose what we are not, period.
- **Tone for IDP-required destinations**: position IDP Companion as the document the reader uses for friction reduction at rental desks, hotel verifications, and police interactions. Mention destination's legal requirement factually (e.g., "Italian law requires foreign drivers to carry an IDP") without then recommending which specific issuer the reader should buy from. The reader can figure out their own due-diligence path; the page's job is to convert.
- **Tone for IDP-not-required destinations (Mexico-style)**: position IDP Companion as the friction reducer at rental desks and informal verification stops. The "no IDP legally required" framing already drives our conversion (skip the AAA visit, save the time, get our companion for translation aid).

**`honesty.isNot` template** (use this exact pattern across all pages):
```
- Not a government-issued IDP under the 1949 Geneva or 1968 Vienna Convention
- Not valid by itself — must be carried alongside your physical home-country driver license
- Not a replacement for [destination-specific local document if relevant — e.g., motorcycle endorsement for Thailand, Mexican TPL insurance for Mexico]
```
**Do NOT add** "Not a substitute for AAA / AATA" or any line that names a competitor issuer in `isNot`. The fact we are not a government IDP is the disclosure; naming the competitor is a recommendation.

**Legal disclaimer template** (single allowed mention of authorized issuers):
```
IDP Companion is a private multilingual translation companion document and is not affiliated with [list relevant government agencies]. IDP Companion is not a government-issued International Driving Permit under the 1949 Geneva Convention or 1968 Vienna Convention. Authorized issuers of US-origin Geneva 1949 IDPs are AAA and AATA. IDP Companion must be used alongside your original [home-country] driver's license.
```
That single sentence in the legal block discharges the legal disclosure obligation. No additional AAA mentions are needed anywhere else on the page.

**`quickAnswer.required` semantics:** `true` for IDP-required destinations (the verdict reads "Yes — you need an IDP in X"). `false` for Mexico-style (verdict reads "No — but...followed by the friction reality").

**Content length:** Tier 1 target = **2,000–2,500 words** in the user-visible copy (excluding TS keys/labels). us-italy = ~2,400, us-mexico = ~2,500, us-japan = ~2,200. Below 1,800 is Tier 2 territory.

**Hero lead structure** (70–150 words):
1. Specific scene-setter (cities, weeks-from-now, booked rental)
2. The killer-angle hook (specific dollar amount + specific incident if available)
3. The legal-vs-real reframe one-liner

The us-mexico hero is the canonical example — it cites May 2025 Tulum ($1,094.50) and Cancún Italian-tourist ($2,566) directly. Specifics outperform generalities for E-E-A-T and CTR.

**SEO title patterns** (pick by destination archetype):
- IDP-required, fines-heavy: `IDP for {Origin} Drivers in {Dest}: {Year} Guide to {Specific Pain} & Avoiding €{Amount}+ Tickets`
- IDP-required, complexity-heavy: `IDP for {Origin} Drivers in {Dest}: {Year} Guide` (subtitle does the work)
- IDP **not** required (Mexico-style): `Driving in {Dest} with a {Origin} License: What '{Counter-narrative}' Actually Costs You ({Year})`

**`ogTitleShort` + `ogSubtitle`** must fit the OG card template — keep ogTitleShort under 50 chars, ogSubtitle under 60.

**Fines table:** include 7–9 rows mixing statutory ranges with documented real-world incidents when verifiable. Every row needs `severity: 'low' | 'med' | 'high'` (drives the colored left-border on cards). Caption must cite source category ("Codice della Strada", "Italian Polizia data", "Ackerman Group reports") — not bare claims.

**Sources block:** cite minimum 3 categories — government source (state department, local ministry of transport), independent third-party (academic journal, news outlet, AAA), and travel-industry data (rental policies, embassy advisories). Bare "everyone knows" facts kill E-E-A-T.

**Related cards (6 items):** target ratio = **3–4 same-origin Live + 1 same-destination Coming Soon + 1 long-form guide Coming Soon stub**. With 5 live US-origin pages now, US-origin pages can show 4 same-origin Live (the 3-same-origin rule from SEO_STRATEGY.md is a soft floor, not a ceiling). Coming-soon stubs render as non-clickable `<div>` — the shared component handles this via `r.href` presence. Always update sibling pages' `related.items` when shipping a new page (add it as Live, drop a stale Coming Soon).

**Hero illustration:** `/illustrations/{origin}-{destination}-hero.svg`. SVG, ~1820×1024 viewBox, brand palette (mint #CBF3F0, sea #2EC4B6, orange #FF9F1C, slate). Drop into `apps/landing/public/illustrations/`.

**OG image:** auto-generated by [components/OgImage/CountryPair.vue](apps/landing/components/OgImage/CountryPair.vue) — `<CountryPairPage>` calls `defineOgImageComponent('CountryPair', { origin: { code, name }, destination: { code, name }, title, subtitle })` automatically. Per-locale variants are cached separately. Don't manually wire `og:image` on the page wrapper.

### Step-by-step: shipping a new country-pair page

1. **Read the brief** (`docs/seo-docs/...` or markdown drop) and identify destination archetype (IDP-required vs not, Latin script vs not, has restriction zones vs not).
2. **Read 2–3 existing pages** matching the archetype as references (e.g. for Tier 1 IDP-required → us-italy + us-japan; for IDP-not-required → us-mexico).
3. **Write `apps/landing/content/country-pairs/{slug}.ts`** with all required + applicable optional blocks. Import `type { CountryPairCopy } from './russia-thailand'`.
4. **Write `apps/landing/pages/idp-for-{origin}-drivers-in-{destination}.vue`** (10–25 lines, mirror an existing wrapper).
5. **Drop hero SVG** at `apps/landing/public/illustrations/{origin}-{destination}-hero.svg`.
6. **Update sibling pages' `related.items`** — add the new page as `status: 'Live', href: '/idp-for-{origin}-drivers-in-{destination}'`. Drop one stale Coming Soon to keep at 6 items.
7. **Verify locally** — `pnpm --filter landing dev`, hit the page, check freshness pill, FAQ accordion, related cards (live ones must navigate, coming-soon stay as `<div>`).
8. **Check `/__og-image__/static/{slug}/og.png`** — confirms OG image renders.
9. **Push to master** — Cloudflare Pages auto-deploys. After ~2 min, `curl https://idpcompanion.com/{slug}` should return 200.

### Content style (validated 2026-04-25)

- Honest framing — every country-pair page must include a "What IDP Companion is / is not / when it helps / when you need official IDP" block. This is our anti-IDA differentiator and Google E-E-A-T signal.
- All facts (fines, speed limits, alcohol limits) MUST be verifiable. When unsure of date specifics or quoted policy language, soften ("recent amendments" instead of specific dates; "rental chains generally require" instead of citing exact policy text).
- Disclaimer + Sources at bottom — name organizations not affiliated with us, then list source references.
- Word count: Tier 1 = 1500-2500 words; Tier 2 = 800-1500.

### Internal linking conventions

- Footer "Pricing" → `/pricing` (standalone), not `#pricing` anchor on home
- Header "Pricing" → `/pricing` (standalone)
- Home has *both* a `#pricing` anchor (scrolls to widget) AND a "View detailed pricing" link to `/pricing`
- Country-pair → 3 same-origin (e.g., Russia→Turkey, Russia→UAE) + 2 same-destination (e.g., US→Thailand, German→Thailand) + 1 guide. **Always render even if targets don't exist yet** as "Coming soon" stubs (text, no `<a>`).

---

## ui-kit — what lives where

> Shared design tokens + components, consumed by both `apps/landing` and `apps/frontend` via Vite alias `@ui-kit`.

### Components

- `BaseButton` — `variant: primary | secondary | ghost`, `size: md | sm`, `as: button | a`, supports `loading`/`disabled`. Use `variant="primary"` for main CTAs (orange), `secondary` for tertiary (slate).
- `BaseInput`, `BaseSelect`, `BaseCountrySelect`, `BaseFile` — form primitives
- `AppHeader` — top nav, slot for `localeSwitcher`. Props for href/labels of How/FAQ/Pricing/Start
- `AppFooter` — bottom nav, takes a `links` object
- `LocaleSwitcher`, `MobileNavMenu` — supporting

### Tokens (SCSS) — [packages/ui-kit/variables.scss](packages/ui-kit/variables.scss)

- Colors as SCSS vars + CSS custom props in `css-vars.scss`. Use `rgb(var(--c-sea))` in scoped styles, or Tailwind classes (`text-sea`, `bg-mint`) — both work.
- Brand: `mint #CBF3F0`, `sea #2EC4B6`, `orange #FF9F1C`, slate scale (50/100/200/400/500/700/900)
- Radii: `$radius-xl: 16px`, `$radius-2xl: 24px`
- Section spacing: `$space-section-y-mobile: 64px`, `$space-section-y-desktop: 80px` (use `.section` class — auto-applies)
- Container: `.container` = max 1280px, responsive padding
- Shadow: `$shadow-soft` standard

### Styling rule

70% Tailwind utilities + 30% BEM-like semantic classes. Don't dump everything into a single Tailwind string. Wrap blocks with `legal-page__heading`, `pricing-plan`, `country-pair-hero` — improves readability and gives anchor for scoped style overrides.

### Landing-only shared components (live in `apps/landing/components/`, not ui-kit)

- `FaqAccordion.vue` — used on home, pricing, country-pair pages. API: `items: Array<{id, question, answer: string | string[]}>`, `defaultOpen?: number`, `multiple?: boolean`, `idPrefix?: string`. Don't recreate accordions — use this.
- `LandingLocaleSwitcher.vue`, `LegalDocumentBody.vue`, `widgets/StartApplicationWidget.vue` — landing-specific

When a component is used by **multiple apps**, it goes to ui-kit. When used only by landing, it stays in `apps/landing/components/`.

---

## Lessons learned (from Phase 0 buildout)

- **Domain stack is fragile.** Canonical URLs were `www.idpcompanion.com` while DNS had no `www` record — Google saw NXDOMAIN for everything. Fix required: (a) add CNAME `www` → apex in Cloudflare DNS, (b) register `www.idpcompanion.com` as Custom Domain in Cloudflare Pages project. Both steps are needed; DNS alone returns 522.
- **Cloudflare's "Manage robots.txt" silently overrides** the static file from `public/robots.txt`. Always check what's actually being served (`curl /robots.txt`) — disable Cloudflare's robots.txt management to keep the static file authoritative.
- **Telegram preview-bot caches OG previews ~2 days.** Use @WebpageBot or append `?v=N` query to bust. New OG tags require this even after deploy.
- **`og:locale` must be `xx_XX` format**, not just `xx`. Use `toOgLocale()` helper.
- **GSC "Couldn't fetch" sitemap is initial state**, not always an error. Google retries on its own schedule. URL Inspection's "Couldn't fetch" with DNS error means real problem.
- **GSC negative DNS cache holds for hours.** After fixing DNS, expect 1-4h before URL Inspection updates. Don't request indexing during this window — it queues against the bad cache.
- **Anthropic-IPs are blocked by Cloudflare's default Bot Score** for proxied hosts (oranges). For diagnostics from Claude Code, fetch via apex (often grey-cloud or routed differently) — `curl https://idpcompanion.com/...` works where `www.` doesn't from the Anthropic side.
- **Windows shell tool quirks:** `curl` is an alias for `Invoke-WebRequest` in PowerShell — use `curl.exe` for real curl. Bash on Windows is fine for unix-style commands.
