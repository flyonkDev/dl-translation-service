```md
# Frontend – Application SPA (Vue 3)

Vue 3 SPA that hosts the actual multi-step application flow.

## Tech

- Vue 3, `<script setup>` + TypeScript
- Vite
- Pinia
- SCSS
- vee-validate + zod
- Feature-Sliced Design flavour:

  - `app/`, `pages/`, `widgets/`, `features/`, `entities/`, `shared/`, `stores/`

## Dev

```bash
cd apps/frontend
npm install
npm run dev
# http://localhost:5173